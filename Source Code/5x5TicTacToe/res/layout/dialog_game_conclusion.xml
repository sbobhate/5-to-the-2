<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/transparency" >
    
	<ImageView 
	    android:id="@+id/conclusion_image"
		android:src="@drawable/dialog_tie"
		android:contentDescription="@string/conclusion_dialog"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:background="#000000"
	    android:layout_centerInParent="true"
	    android:layout_marginLeft="20dp"
	    android:layout_marginRight="20dp" />
	
	<ImageView
	        android:id="@+id/newgame_button"
	        android:src="@drawable/ic_action_replay"
	        android:contentDescription="@string/new_game_button"
	        android:layout_width="wrap_content"
	        android:layout_height="wrap_content"
	        android:layout_below="@+id/conclusion_image"
			android:layout_centerHorizontal="true" />
    
</RelativeLayout>
